{
  "gitSyncId": "69db7f624e558177144e639c_fetch_leads_by_type_period_01",
  "id": "stats_fetch_leads_by_type_period",
  "pluginId": "postgres-plugin",
  "pluginType": "DB",
  "unpublishedAction": {
    "actionConfiguration": {
      "body": "SELECT \n  l.type AS סוג_ליד,\n  COUNT(DISTINCT CASE WHEN DATE(l.created_at) = CURRENT_DATE \n    THEN c.identity_number END) AS היום,\n  COUNT(DISTINCT CASE WHEN l.created_at >= DATE_TRUNC('week', CURRENT_DATE) \n    THEN c.identity_number END) AS השבוע,\n  COUNT(DISTINCT CASE WHEN l.created_at >= DATE_TRUNC('month', CURRENT_DATE) \n    THEN c.identity_number END) AS החודש\nFROM leads l\nJOIN customers c ON l.customer_id = c.id\nWHERE l.type != 'בדיקת תיק מלאה'\nAND l.type IS NOT NULL\nGROUP BY l.type\nORDER BY החודש DESC;",
      "encodeParamsToggle": true,
      "paginationType": "NONE",
      "pluginSpecifiedTemplates": [
        {
          "value": false
        }
      ],
      "timeoutInMillisecond": 10000
    },
    "confirmBeforeExecute": false,
    "datasource": {
      "id": "Supabase",
      "isAutoGenerated": false,
      "name": "Supabase",
      "pluginId": "postgres-plugin"
    },
    "dynamicBindingPathList": [
      {
        "key": "body"
      }
    ],
    "name": "fetch_leads_by_type_period",
    "pageId": "stats",
    "runBehaviour": "AUTOMATIC",
    "userSetOnLoad": true
  }
}